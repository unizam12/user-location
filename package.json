{
  "name": "user-location-project",
  "version": "1.0.0",
  "description": "user location project",
  "main": "app.js",
  "author": "unizam12 <21100095@lums.edu.pk>",
  "license": "MIT",
  "scripts": {
    "start": "cd ./server node app.js",
    "server": "nodemon server/app.js",
    "client-start": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd ./client && npm run build",
    "install-client": "cd ./client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build && cd ./server npm install && cd .."
  },
  "dependencies": {},
  "devDependencies": {
    "express": "^4.17.1"
  }
}
